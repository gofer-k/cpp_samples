import deducing_this;
import callable_features;
import forwarding_types;

int main() {
  deducing_this::test_deducing_this();
  callable_features::test_callable_features();
  forwarding_types::test_forward_like();
  forwarding_types::test_forwarding_types();
}